# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Dependency directories
node_modules/
.npm/
.pnpm-store/

# Optional eslint cache
.eslintcache

# Build artifacts
build/              # General build directory
dist/               # General distribution directory
release/            # Electron-builder output directory for installers/executables
build-app/          # Electron-builder's intermediate app directory (where React build is moved)
app/dist/           # React app's build output directory
app/build/          # Alternative React app build output directory

# macOS specific files
.DS_Store
*.orig
*.dmg
*.pkg
*.app

# Windows specific files
*.exe
*.dll
*.exp
*.lib
*.pdb
*.zip
*.tar.gz
out/                # Common Electron build output for unpacked apps on Windows
electron-build/     # Another possible Electron build directory

# Linux specific files
*.AppImage          # Electron-builder output for AppImage

# Local configuration files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local
app-config.json     # Application runtime configuration (should not be committed)
vault.dat           # Encrypted vault data file (should not be committed)

# IDE specific files
.vscode/            # Visual Studio Code specific files
.idea/              # IntelliJ IDEA / WebStorm specific files
*.sublime-project
*.sublime-workspace

# Electron-specific ignores
*.asar
*.asar.unpacked
# Explicitly keep icon files if they are part of your source and needed for builds
!icon.ico
!icon.icns
!icon.png
# If you have icon files that are generated or temporary, you can ignore them like:
# icon.ico*

# Thumbnails
.thumbnails/

# Crash reports
Crashpad/
